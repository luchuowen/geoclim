// TEMPORARY scaffold-verification placeholder — owned by Session 1
// (docs/04_session_1_homepage.md) once that session starts. This file
// exists only so `next build`/`next dev` have a route to render while
// verifying the bare scaffold; Session 1 overwrites it with the real
// homepage and should not treat this content as a starting point.

export default function Home() {
  return (
    <main>
      <p>GeoClim East Africa — scaffold placeholder. Session 1 builds the real homepage here.</p>
    </main>
  );
}
